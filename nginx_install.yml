---
- hosts: all
  vars:
    ansible_python_interpreter: /usr/bin/python3
  tasks:
    - name: Instal nginx
      yum:
        name: nginx
        state: present
    - name: start nginx
      service:
          name: nginx
          state: started
